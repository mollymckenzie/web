---
import Layout from '@layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { extractThemes } from '@lib/data/libraryAccess';
import { withBasePath } from '@lib/paths';

const resources = await getCollection('master-library');
const dataThemes = extractThemes(resources, 'community');
---

<Layout
  title="Community Member"
  description="Community-focused pathways for finding and sharing locally relevant data resources."
>
  <section class="role-shell">
    <header class="role-header">
      <div class="role-icon">
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
          <circle cx="9" cy="7" r="4"/>
          <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
          <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
        </svg>
      </div>
      <div>
        <h1>Community Member View</h1>
        <p class="role-desc">
          Find and share resources that reflect local context, community priorities, and practical knowledge from the field.
        </p>
      </div>
    </header>

    <a href={withBasePath('/complete-catalog?audience=community')} class="button cta-button">Open Complete Catalog as Community Member &rarr;</a>

    <article class="panel">
      <h2>Available Data Themes</h2>
      {dataThemes.length > 0 ? (
        <ul>
          {dataThemes.map((theme) => (
            <li>{theme}</li>
          ))}
        </ul>
      ) : (
        <p class="empty-note">No themes available yet.</p>
      )}
    </article>

    <div class="info-card">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="12" cy="12" r="10"/>
        <line x1="12" y1="8" x2="12" y2="12"/>
        <line x1="12" y1="16" x2="12.01" y2="16"/>
      </svg>
      <p>Some datasets are restricted to teacher view only. If you need access to a specific resource, contact your library administrator.</p>
    </div>
  </section>

  <style>
    .role-shell {
      display: grid;
      gap: var(--space-6);
      max-width: 720px;
    }

    .role-header {
      display: flex;
      align-items: flex-start;
      gap: var(--space-4);
    }

    .role-icon {
      width: 52px;
      height: 52px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: color-mix(in srgb, var(--color-primary) 10%, var(--color-surface));
      border-radius: var(--radius-lg);
      color: var(--color-primary);
      flex-shrink: 0;
    }

    .role-header h1 {
      margin-bottom: var(--space-2);
    }

    .role-desc {
      color: var(--color-text-muted);
      font-size: var(--font-size-lg);
      margin: 0;
    }

    .cta-button {
      display: inline-flex;
      align-items: center;
      align-self: start;
    }

    .panel {
      border: 1px solid var(--color-border);
      background: var(--color-surface);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
    }

    .panel h2 {
      margin-bottom: var(--space-4);
      font-size: var(--font-size-base);
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      color: var(--color-text-muted);
    }

    .panel ul {
      margin: 0;
      padding-left: 1.25rem;
      display: grid;
      gap: var(--space-2);
      font-size: var(--font-size-sm);
    }

    .empty-note {
      margin: 0;
      color: var(--color-text-muted);
      font-size: var(--font-size-sm);
    }

    .info-card {
      display: flex;
      gap: var(--space-3);
      align-items: flex-start;
      padding: var(--space-4) var(--space-5);
      background: color-mix(in srgb, var(--color-primary) 6%, var(--color-surface));
      border: 1px solid color-mix(in srgb, var(--color-primary) 20%, var(--color-border));
      border-radius: var(--radius-lg);
      color: var(--color-primary);
    }

    .info-card svg {
      flex-shrink: 0;
      margin-top: 2px;
    }

    .info-card p {
      margin: 0;
      font-size: var(--font-size-sm);
      color: var(--color-text-muted);
      line-height: var(--line-height-relaxed);
    }
  </style>
</Layout>
